name: knowledge_graph
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  data_layer: ^0.0.6+1
  flutter:
    sdk: flutter
  logging: ^1.3.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.6.4
  build_runner: ^2.14.1

flutter:
  uses-material-design: true
